import { Container } from "@mantine/core";
import { BaseLayout } from "../layout";

/**
 * This page is just for testing the match map.
 * 
 * 
 * I tried <https://www.w3schools.com/htmL/html_images_imagemap.asp>'s first, but it did not work.
 */
export default function DevPage() {
    return <BaseLayout>
        <Container size="xl">

        </Container>
    </BaseLayout>
}